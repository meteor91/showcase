import {routeMap as themesRouteMap} from 'apps/themes/routeMap';
import {routeMap as usersRouteMap} from 'apps/users/routeMap';

export const routeMap = {
    themes: themesRouteMap,
    users: usersRouteMap,
};
